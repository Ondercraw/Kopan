# Paquetes a instalar para el módulo de autenticación

## Backend
cd backend
npm install @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt cookie-parser
npm install -D @types/passport-jwt @types/bcrypt @types/cookie-parser

## Frontend
No hace falta instalar nada nuevo: ReactiveFormsModule y HttpClient ya vienen con Angular.
